import React from 'react';
import ReactLoading from 'react-loading';

const Loading = ({ type, color }) => (
  <ReactLoading type={this.bubbles} color={this.red} height='667' width='375' />
);

export default Loading;
